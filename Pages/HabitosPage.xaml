<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="MauiAppAMASBE.Pages.HabitosPage"
    Title="Hábitos">

    <Grid>

        <!-- Estado vazio -->
        <VerticalStackLayout 
            x:Name="EmptyState"
            Spacing="20"
            VerticalOptions="Center"
            HorizontalOptions="Center">

            <StackLayout>
                <Frame Padding="0" Margin="10" VerticalOptions="FillAndExpand">
                    <ListView x:Name="lst_habito" 
                 IsPullToRefreshEnabled="True"
                  Refreshing="lst_habito_Refreshing"
                              ItemSelected="lst_habito_ItemSelected">
                        <ListView.Header>
                            <Grid ColumnDefinitions="*, *, *, *, *, *, *,*"
                                  BackgroundColor="{StaticResource Gray100}">
                                <Label Grid.Column="0" Text="ID" Padding="5" FontAttributes="Bold" HorizontalOptions="Center"/>
                                <Label Grid.Column="1" Text="Nome do Hábito" FontAttributes="Bold" HorizontalOptions="Center"/>
                                <Label Grid.Column="2" Text="Descrição" FontAttributes="Bold" HorizontalOptions="Center"/>
                                <Label Grid.Column="3" Text="Frequencia" FontAttributes="Bold" HorizontalOptions="Center"/>
                                <VerticalStackLayout  Grid.Column="4" >
                                    <Label Text="Meta" FontAttributes="Bold" HorizontalOptions="Center"/>

                                    <Label Text="(n°/unidade)" FontAttributes="Bold" HorizontalOptions="Center"/>
                                </VerticalStackLayout>
                                <Label Grid.Column="5" Text="Início" FontAttributes="Bold" HorizontalOptions="Center"/>
                                <VerticalStackLayout Grid.Column="6">
                                    <Label  Text="Nº etapas " FontAttributes="Bold"
                                            HorizontalOptions="Center"/>
                                <Label Text="concluidas" FontAttributes="Bold"
                                       HorizontalOptions="Center"/>
                                </VerticalStackLayout>
                                <Label Grid.Column="7" Text="Progresso" FontAttributes="Bold" HorizontalOptions="Center"/>
                            </Grid>
                        </ListView.Header>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <ViewCell.ContextActions>
                                        <MenuItem Text="Remover" Clicked="MenuItem_Remover_Habito"/>
                                        <MenuItem Text=" Editar" Clicked="MenuItem_Editar_Habito"/>
                                    </ViewCell.ContextActions>
                                    <Grid ColumnDefinitions="*, *, *, *, *, *, *,*">
                                        <Label Grid.Column="0" Text="{Binding IdHabito}"
                                               HorizontalOptions="Center"
                                               VerticalOptions="Center"/>
                                        <Label Grid.Column="1" Text="{Binding NomeHabito}"
                                               HorizontalOptions="Center"
                                               VerticalOptions="Center"/>
                                            <Label Grid.Column="2" Text="{Binding DescricaoHabito}"
                                                   HorizontalOptions="Center"
                                                   VerticalOptions="Center"/>
                                        <Label Grid.Column="3" Text="{Binding FrequenciaHabito}"
                                               HorizontalOptions="Center"
                                               VerticalOptions="Center"/>
                                        <HorizontalStackLayout HorizontalOptions="Center" Grid.Column="4">
                                        <Label  Text="{Binding MetaValor}" Margin="5"
                                              
                                               VerticalOptions="Center"/>
                                        <Label Text="{Binding MetaUnidade}"
                                               
                                               VerticalOptions="Center"/>
                                        </HorizontalStackLayout>
                                        <Label Grid.Column="5" Text="{Binding HorarioHabito}"
                                                           HorizontalOptions="Center"
                                                           VerticalOptions="Center"/>
                                        <HorizontalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Grid.Column="6">
                                            <Label  Text="{Binding ValorAtual}"
                                                    HorizontalOptions="Center" Margin="5"/>
                                            <Stepper   Minimum="0"
                                                  Maximum="{Binding MetaValor}"
                                                  Increment="1"
                                                  Value="{Binding ValorAtual}"
                                                  ValueChanged="Stepper_ValueChanged"
                                                       HorizontalOptions="Center"/>
                                            
                                        </HorizontalStackLayout>
                                         <Label Grid.Column="7" Text="{Binding Progresso, StringFormat='{0:F0}%'}"
                                                HorizontalOptions="Center"
                                                VerticalOptions="Center"/>
                                    </Grid>
                                </ViewCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Frame>
            </StackLayout>

            <Label Text="📋"
                       FontSize="50"
                       HorizontalOptions="Center" />

            <Label Text="Crie novos hábitos!"
                       FontSize="16"
                       TextColor="#666"
                       HorizontalTextAlignment="Center" />

            <Button 
                Text="+ Novo Hábito"
                Clicked="OnNovoHabitoClicked"
                BackgroundColor="#6A0DAD"
                TextColor="White"/>
        </VerticalStackLayout>

        <!-- Card de cadastro -->
        <Frame 
            x:Name="CadastroCard"
            IsVisible="False"
            Padding="20"
            CornerRadius="15"
            BackgroundColor="#F5F5F5"
            Margin="20"
            VerticalOptions="Center">

            <VerticalStackLayout Spacing="10"
              BackgroundColor="{StaticResource Gray100}"
                                 Padding="10"
Margin="10">


                <Label 
                    Text="Novo Hábito"
                    FontSize="20"
                    FontAttributes="Bold"/>

                <Entry 
                    x:Name="NomeEntry"
                    Placeholder="Nome do hábito"/>

                <Entry 
                    x:Name="TipoEntry"
                    Placeholder="Tipo (Ex: Exercício)"/>
                <Entry 
                    x:Name="DescricaoEntry"
                    Placeholder="Descrição(Ex: Caminhar por 1h hora)"/>
                <TimePicker 
                      x:Name="timeHorario"
                      Format="HH:mm"/>
                <HorizontalStackLayout
                    Padding="0"
                    Margin="10"
                    Spacing="20">
                    <Entry 
                x:Name="MetaEntry"
                Placeholder="Meta N° (Ex:5.5)"
                    Keyboard="Numeric"/>
                    <Picker 
                Title="Unidade"
                    BackgroundColor="{StaticResource Gray100}"
                 ItemsSource="{Binding Unidades}"
                SelectedItem="{Binding UnidadeSelecionada}" />

                <Picker x:Name="pck_frequencia"
                Title="Selecione a frequência"
                ItemsSource="{Binding Frequencias}"
                SelectedItem="{Binding FrequenciaSelecionada}" />
                </HorizontalStackLayout>

                <Button 
                    Text="Salvar"
                    Clicked="OnSalvarHabitoClicked"
                    BackgroundColor="#6A0DAD"
                    TextColor="White"/>

                <Button 
                    Text="Cancelar"
                    Clicked="OnCancelarClicked"
                    BackgroundColor="Gray"
                    TextColor="White"/>

            </VerticalStackLayout>
        </Frame>
        
        <!--Card de edição-->
        <Frame 
    x:Name="EditCard"
    IsVisible="False"
    Padding="20"
    CornerRadius="15"
    BackgroundColor="#F5F5F5"
    Margin="20"
    VerticalOptions="Center">

            <VerticalStackLayout 
        Spacing="10"
        BackgroundColor="{StaticResource Gray100}"
        Padding="10"
        Margin="10">

                <Label 
            Text="Editar Hábito"
            FontSize="20"
            FontAttributes="Bold"/>

                <!-- Nome (IMPORTANTE adicionar) -->
                <Label 
            Text="{Binding NomeHabito}"/>

                <!-- Horário -->
                <TimePicker 
            x:Name="Edit_timeHorario"
            Format="HH:mm"/>

                <HorizontalStackLayout
            Padding="0"
            Margin="10"
            Spacing="20">
                    <VerticalStackLayout>
                    <!-- Meta -->
                    <Label
                        Text= "N° de vezes"/>
                        <Entry 
                x:Name="Edit_MetaEntry"
                Placeholder="Meta (Ex: 5.5)"
                Keyboard="Numeric"/>
                    </VerticalStackLayout>


                    <!-- Frequência -->
                    <Picker 
                x:Name="Edit_pck_frequencia"
                Title="Selecione a frequência"
                ItemsSource="{Binding Frequencias}"
                SelectedItem="{Binding FrequenciaSelecionada}" />
                </HorizontalStackLayout>

                <!-- Botão salvar -->
                <Button 
            Text="Salvar"
            Clicked="Button_Editar_Habito"
            BackgroundColor="#6A0DAD"
            TextColor="White"/>

                <!-- Cancelar -->
                <Button 
            Text="Cancelar Edição"
            Clicked="Button_Cancelar_edicao"
            BackgroundColor="Gray"
            TextColor="White"/>

            </VerticalStackLayout>
        </Frame>

    </Grid>

</ContentPage>